version: '1'
services:

  pg:
    image: postgres:13.3
    env_file:
      - ".env"
    # environment:
    #   - POSTGRES_USER=${DB_USERNAME}
    #   - POSTGRES_PASSWORD=${DB_PASSWORD}
    #   - POSTGRES_DB=${DB_NAME}
    ports:
      - "5432:5432"

  redis:
    image: redis
    ports:
      - "6379:6379"